# M1 작업계획서 — 기반 (스캐폴딩·read API·runs reader)

> 근거 설계 §2·§5·§7 M1. DoD 수용기준: **A1, A3api, A5be**. 게이트: API 단위테스트 + 외부감사.

## 선행 (교차 관심사 확립)
- [ ] `harness-ui/` 스캐폴딩(§앱 구조): `src/web`(React/Vite/TS)·`src/server`(Fastify)·`src/server/{api,adapters,schemas}`
- [ ] **schema 모듈**(§5 Zod) 배치: manifest/status/events/agents/DriftFinding + **`Usage`(부분)·ISO datetime(+offset)** 포함 + `schema-valid = X.parse()` 헬퍼 + 스키마 스냅샷 테스트
- [ ] **3-OS CI 워크플로**(`.github/workflows/ci.yml`) 생성(Linux/macOS/Windows: typecheck·단위·이후 마일스톤 회귀). M2 게이트 전제
- [ ] `<state_home>` 경로 어댑터(§9-STATE, macOS/Linux/Windows)
- [ ] `package.json` scripts(dev/dev:web/dev:server/build/typecheck) + package-lock 커밋
- [ ] 보안 미들웨어 훅 자리(token/Host/Origin — 구현은 M4, 골격만)

## 작업
- [ ] `GET /api/runtimes` — claude/codex/agy 설치·버전·경로. **바이너리 해소는 shell 없이**(`which`/Windows `where.exe`를 execFile) 후 절대경로 `--version`(§execFile no-shell — `command -v` 금지)
- [ ] `GET /api/harness` — 인벤토리: claude(entrypoint/agents/skills count)·codex·workspace(runs)
- [ ] `GET /api/agents`·`/api/agents/:name` — `.claude/agents/*.md`·`.codex/agents/*.toml` 파싱(name·runtime·role·tools·skills)
- [ ] `GET /api/skills`·`/api/skills/:name` — SKILL.md frontmatter·references·runtimePaths
- [ ] runs reader — `_workspace/runs/**` 스캔(없거나 비어도 안전), Zod parse로 schema-valid만 노출
- [ ] route param 검증(SAFE_SEGMENT `^[A-Za-z0-9._-]+$`) 골격
- [ ] `npm run dev`로 web+server 동시 기동(concurrently)

## 수용기준 (DoD)
- [ ] **A1** `npm run dev` → UI+API 동시 실행(dev proxy same-origin)
- [ ] **A3api** inventory API가 agents/skills count 반환(단위테스트)
- [ ] **A5be** `_workspace/runs` 없/빈 → reader crash 없이 빈 목록

## 테스트/fixture
- [ ] `test/fixtures/runs/` — 빈·정상(schema-valid) run
- [ ] API 단위테스트(각 엔드포인트 shape = Zod)

## 게이트
- [ ] 결과서 기록 + `check-artifacts` PASS
- [ ] 외부감사(codex+agy) → 확인분 수정 → PASS
- [ ] typecheck GREEN

## 다음 단계 참조
- M2 착수 조건: schema 모듈·state_home·서버 골격 완비. runs reader가 M2 supervisor 산출 스키마를 읽으므로 스키마 계약 고정 필수.
