# M5 작업계획서 — 실행 인증 (dry-run·실 스모크)

> 근거 §Codex/Claude 실행 전략·§6. DoD: **A6,A9b,A15,A16,A17,A17w**. 게이트: 3-OS 실 스모크 + 외부감사.

## 작업 — 실행 API
- [ ] `POST /api/runs`(Zod 검증): runtime(claude|codex)·mode·domain(길이)·permissionMode(allowlist)·model·targets(allowlist)·dryRun
- [ ] `POST /api/runs/:runId/cancel` · events cursor · artifacts
- [ ] **Build dry-run**(A9b): 입력→계획 생성·표시, **파일 수정 없음**
- [ ] 서버 실행 계약(§실행 전략): `execFile`+argv, `--`+positional `safePrompt`(`Task:\n` prefix), `detached`, `HARNESS_RUN_DIR` 주입
  - codex: `codex exec --json --ignore-user-config --sandbox <mode> -o <out> -- <prompt>`
  - claude: `claude -p [--model] --permission-mode <m> --allowedTools <t...(배열)> -- <prompt>`
- [ ] Runs 화면 e2e(A6): 실 run 이벤트·agent status·산출물 표시

## 수용기준 (DoD)
- [ ] **A6** 실 run e2e → Runs 화면 이벤트·agent status
- [ ] **A9b** Build dry-run 데이터 생성·표시(파일수정 없음)
- [ ] **A15** shell metachar domain 입력이 host shell 미실행(execFile 증명)
- [ ] **A16** prompt `Task:\n` prefix + `--` positional(claude/codex) — dash 입력 옵션해석 안 됨
- [ ] **A17** 실 codex/claude 스모크(opt-in) → schema-valid status/events/agent (mac/linux)
- [ ] **A17w** 동 Windows

## 테스트/fixture
- [ ] opt-in 스모크(비용/모델 경고·타임아웃·read-only sandbox)·CI mock 대체
- [ ] injection 페이로드(`; rm`·`$()`·dash-prefix domain) 미실행 검증
- [ ] **argv 스냅샷 테스트**: `--allowedTools`가 배열(콤마 문자열·`--tools` 거부), `--` 앞 옵션·뒤 positional 순서 고정
- [ ] 3-OS 스모크 매트릭스

## 게이트
- [ ] 결과서 + `check-artifacts` PASS · 외부감사 PASS · 3-OS CI GREEN
- [ ] **코어(M1-M5) 완료 = v0.5 실행 기능 완성**(런처 제외)

## 다음 단계 참조
- M6(런처) 착수 조건: **런처 설계 R7 인증 통과**(현재 대기). 미통과면 M5까지가 v0.5 배포 단위(수동 `npm install`+`npm run dev`). 런처는 M5 산출 앱을 대상으로 bootstrap.
