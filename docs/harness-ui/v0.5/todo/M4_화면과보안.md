# M4 작업계획서 — 화면 + 보안

> 근거 §5b·§2(DESIGN.md)·README IA. DoD: **A2,A3,A4,A4b,A5,A7,A8,A8b,A9a,A10,A11,A12,A13,A14,A23,A26,A27,A28,A29**. 게이트: 보안 스위트 + 외부감사.

## 작업 — 화면(DESIGN.md=Linear 토큰 기준)
- [ ] **DESIGN.md 토큰 구현**: CSS 변수(colors `#010102`/`#5e6ad2` 등·surface·hairline)·타이포(negative tracking)·다크 기준 → 디자인시스템 모듈
- [ ] 좌 사이드바(Overview/Build/Agents/Skills/Runs/Drift/Ops/Settings)·상단바·데스크톱 우선
- [ ] **Overview**(A2): 런타임 설치·버전·drift 요약·최근 run
- [ ] **Agents/Skills**(A3): count·목록·상세(선언 정보)
- [ ] **Drift**(A4·A4b): `.codex/agents` 누락 + full(CLAUDE↔AGENTS·skills desc·refs 누락) 표시
- [ ] **Ops**(A7·A8·A8b): 설치·버전·헬스 / `/usage`·`/status` 참조카드+사유+snapshot 경로 / snapshot import(raw 보존·normalized·unparsed 표시)
- [ ] **Build 화면 골격**(A9a): 입력폼·미리보기 레이아웃(데이터는 M5)
- [ ] **Runs**(A5): 빈 runs UI 안 깨짐
- [ ] **Overview 상태·통계 계층 A**(`design-observability §7b`) — **백엔드 collector `GET /api/overview/state-stats`**(typed·Zod) + per-A35-A38 fixture + 파서 단위테스트 + UI assertion(라벨만 렌더 방지):
  - [ ] A35 구성 건강도: 고아 에이전트/스킬(연결 0)·오케스트레이터 유무·CLAUDE.md/AGENTS.md 포인터 정합(measured) + 에이전트↔스킬 커버리지(**heuristic 라벨** — 정의 파싱)
  - [ ] A36 D4 규율: **TS 네이티브 파일 검사**(결과서 유무·`## 다음 단계 참조` 정규식·`_workspace` 방치물 스캔 — check-artifacts 셸아웃 금지, 스크립트 부재 무관)
  - [ ] A37 업데이트 상태: `.harness-manifest.json` 파싱(USER-MODIFIED 수·보류=measured). manifest 없으면 `no-manifest`, factory 경로 미설정 시 factory-drift=`unknown`(실패 아님)
  - [ ] A38 진화 이력: CLAUDE.md **+ AGENTS.md**(듀얼) 변경이력 파싱 타임라인
- [ ] 상태는 색+label 병행, 긴 로그 접힘+필터

## 작업 — 보안 (§5b)
- [ ] **token bootstrap**(A12): **CSPRNG ≥128bit**·single-use bootstrap→session 교환+즉시 무효화·`<state_home>` 0600/ACL 저장·HTML 미주입·쿼리토큰 금지·로그/referrer/query 미노출(음성 테스트)
- [ ] Host allowlist(A11: **모드별 정확** `127.0.0.1:{port}`·`localhost:{port}`·**`[::1]:{port}`**(IPv6 괄호 표기))·Origin(state-mutating)·CORS 단일 origin·**dev-proxy Host/trust-proxy 명시 설정**(Fastify↔Vite, 신뢰 헤더만)
- [ ] **cross-origin POST 거부**(A10: token+Origin+Host)
- [ ] **path traversal 거부**(A13): SAFE_SEGMENT·`..`·URL-decode 재검·realpath 경계
- [ ] **denylist**(해석 전): `.ui-session-token`·dotfile·레지스트리·secret
- [ ] **artifact 서빙**(A28 positive / A14·A27 negative): 불변 스냅샷·**세그먼트 `lstat` 심링크 거부·leaf `O_NOFOLLOW`·`S_ISREG` 확인**·`fs.open`→`fstat`→fd 스트리밍·**바이트 크기 상한**·제어문자 escape·다운로드 파일명 sanitize·nosniff·attachment/text
- [ ] **live update**(A23): header 인증 polling/fetch stream(쿼리토큰 SSE 금지)
- [ ] **Ops command API**(A26): client args/env/cwd/command 거부(commandId만)
- [ ] **drift sync-plan**(A29): 파일 미변경(계획만)
- [ ] 페이지네이션: `after` exclusive·`limit` clamp·`{items,nextAfter,hasMore,...}`

## 작업 — 배선·UX 보강 (감사 반영)
- [ ] **API 배선**: `GET /api/settings`(A39·read-only) · `GET /api/overview/state-stats`(A40·typed) · bootstrap 교환(A41) · `POST /api/runs` 페이로드 리소스제한·권한·모델·targets(A42) · Skills `triggers`·Agents 최근상태 join·Runs 재시도필드(A43) · drift sync-plan 응답 스키마(A44)
- [ ] **artifact UX**(A45): list·escaped preview·attachment 다운로드 버튼·빈 상태
- [ ] **UX 표준**(A46): 빈/로딩/에러 3-state·action 에러 토스트+retry(GET만·POST 확인)·cancel 멱등(terminal/409)·polling interval/backoff·키보드 nav·색+label 접근성·모바일 읽기전용·위험작업(run/drift-apply) dry-run 확인

## 수용기준 (DoD)
- [ ] A2,A3,A4,A4b,A5,A7,A8,A8b,A9a (화면)
- [ ] A35,A36,A37,A38 (Overview 상태·통계 계층 A)
- [ ] A39,A40,A41,A43,A44,A45,A46 (배선·UX 보강 / A42 폼=M4·실행배선=M5)
- [ ] A10,A11,A12,A13,A14,A23,A26,A27,A28,A29 (보안·API)

## 테스트/fixture
- [ ] 보안 스위트: traversal payload·cross-origin·token 없는 GET·Host 벗어난 GET·심링크 artifact·HTML/SVG artifact 비실행
- [ ] 토큰 경화 테스트: 엔트로피(≥128bit)·토큰파일 mode/ACL·서버로그/referrer/query 미노출·bootstrap 1회성 소진
- [ ] snapshot import raw 샘플(파싱 성공/실패)

## 게이트
- [ ] 결과서 + `check-artifacts` PASS · **security-review** · 외부감사 PASS

## 다음 단계 참조
- M5 착수 조건: 화면·보안·artifact 서빙 완비. M5가 실 실행으로 Runs/Build를 e2e 채움. 보안 경계(token·path)가 실행 API에 그대로 적용됨.
