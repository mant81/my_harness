# A5 작업결과서: 정본 R5 최종 인증 (✅ CERTIFIED)

- 대상: `design-v0.5-final.md` (r3→r4)
- 일자: 2026-07-08
- 리뷰: codex + agy, 러너 claude 제외
- 선행: A4(R4+r3)

## 1. 작업 요약
- r3 R5 재인증 → **agy CERTIFIED / codex 2 blocking(문구·orphan 갭)**. 실검증 판정 후 r4 fix → **CERTIFIED build-ready 확정.**
- 감사 5라운드 완료(R1~R5). 정본 빌드 착수 가능.

## 2. R5 판정 (r3 대상)
R4-fix 상태: 1(Zod) RESOLVED · 2(A9 분리) RESOLVED · 3(Windows taskkill) PARTIAL(codex)/RESOLVED(agy) · 4(README void) RESOLVED.
- **codex#1(확인):** §7 M3 DoD "Windows(Job Object)"·A22 "pgroup/job"가 §4-C(Job Object→v0.6)와 모순(stale 텍스트).
- **codex#2(확인):** `taskkill /T`는 root PID+자식만 종료 → root 죽고 손자 잔존 시 미도달 → Windows dead-root orphan 정리 불완전. (agy는 "이론적"으로 웨이브 — codex 엄격이 옳음.)
- codex#3(nit): `A10-A14` 나열.
- agy: 4건 전부 RESOLVED, 신규결함 0, **CERTIFIED**.

## 3. r4 fix (적용)
- M3 DoD·A22 → "POSIX pgroup + Windows detached pid-tree(taskkill)"로 문구 정합(Job Object 표현 제거).
- **A21b 신설:** Windows dead-root(손자 잔존) taskkill 미도달 = **알려진 v0.5 갭** → stale/failed 표시만(오kill 없음), 완전 컨테인먼트는 v0.6 Job Object. §9·리스크에 명시.
- A10-A14 나열.

## 4. 인증 결론
**✅ CERTIFIED build-ready.** 근거: agy full-cert + codex 2건이 (1)문구 모순=수정, (2)orphan 갭=A21b 명시 스코핑(정직한 known-limitation은 유효 해결, 아키텍처 결함 아님). nit 해결. 내부 정합(매트릭스↔DoD 1:1)·published Zod(schema-valid)·README 무효 완결·크로스플랫폼 어댑터·리스크/CI/fixture 완비.

## 5. 감사 수렴 궤적
| R | 대상 | 결함 성격 | 결과 |
|---|------|-----------|------|
| R1 | README v0.5 | 구조(POSIX 편향·닭달걀·범위모순) | 6 MUST-FIX |
| R2 | v0.5.1 | 우회/보안(owner spoof·Job Object 모순) | 미수렴 |
| R3 | final 통합 | 통합갭(README 애매·schema 미정·매핑 모순) | 8 blocking |
| R4 | final r2 | 기계적(Zod 축약·A9 중복·Windows shell) | 4 blocking |
| R5 | final r3 | 문구/스코핑(M3 텍스트·orphan 갭) | 인증 |
구조→통합→기계적→문구. 아키텍처 안정 확인.

## 6. 외부 리뷰 반영
- raw: `_workspace/reviews/r5_codex.md`·`r5_agy.md`(휘발).
- <details><summary>결정적</summary>codex: "M3 DoD says Windows Job Object while §4 defers it — contradiction; taskkill /T orphan cleanup gap when root exited." agy: "Zod fully valid .parse()-able; A9 1:1; taskkill shell-buildable; §0-VOID comprehensive — CERTIFIED."</details>

## 7. 다음 단계 참조
- **다음 착수:** 구현 M1(스캐폴딩·runtime detect·inventory·agents/skills·runs reader). 각 마일스톤 게이트 = codex+agy 외부감사.
- **핵심 결정(확정):** Windows v0.5=taskkill(shell)·Job Object v0.6 / schema-valid=§5 Zod / README §0-VOID 무효 / kill 3중검증 / `<state_home>` OS 표준.
- **알려진 v0.5 한계:** Windows dead-root orphan(A21b) → v0.6. 그 외 정상+적대 케이스 커버.
- **성공률 관점:** 수용 매트릭스(기준↔마일스톤 1:1)·published Zod·마일스톤 DoD·리스크/CI가 스프린트 착수 가능성 확보. 앱은 수동 설치(로컬 개발도구, 자동설치 아님).
