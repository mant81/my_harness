# A4 작업결과서: 정본 r2 R4 재인증 + r3 fix

- 대상: `design-v0.5-final.md` (r2→r3)
- 일자: 2026-07-08
- 리뷰: codex + agy, 러너 claude 제외
- 선행: A3(R3, 정본 r2 blocking 8건)

## 1. 작업 요약
- r2(A3 8건 반영) R4 재인증 → **NOT certified**, 단 findings 전부 기계적/정합(구조 문제 없음).
- r3 fix 적용: 실 Zod·A9 분리·Windows taskkill·README 무효 확대·필드명 정합·lstart 불투명·usage nullable.

## 2. R4 판정 (r2 대상)
R3-fix 상태: 1 PARTIAL · 2 RESOLVED · 3 NOT · 4 RESOLVED · 5 PARTIAL · 6 RESOLVED · 7 RESOLVED · 8 PARTIAL.
**Blocking(수렴):**
- ① §5 Zod가 축약 pseudo-code(비실행) — `z.array` 타입 누락, 다수 필드 무타입. schema-valid 실행 불가.
- ② A9 M4&M5 중복(1:1 위반) — 매트릭스 M5인데 M4 DoD에 "A9(표시 골격)".
- ③ **Windows Job Object shell 불가** — `TerminateJobObject`/named job은 Win32 API, PowerShell은 Add-Type P/Invoke=사실상 native → "shell 확정" 모순.
- ④ README §남은결정(996-1001)·acceptance 미무효화.
**High:** 필드명 `childGroupOrJob`↔README `childProcessGroupId` 이중 · `ps lstart` 파싱 취약 · usage nullable 정책.

## 3. r3 fix (적용 완료)
1. §5 → **완전 실행 Zod**(모든 필드 타입, `z.array(z.string())`, iso datetime, RunState/Runtime enum, Usage.partial, childProcessGroupId=union nullable). schema-valid=`.parse()` 통과.
2. A9 → **A9a(M4 화면 골격)/A9b(M5 dry-run 데이터)** 분리, 매트릭스·DoD 1:1.
3. Windows → **v0.5=`taskkill /T` 트리 종료(shell) + 레지스트리+identity 3중**, Job Object=v0.6 native 이관(정직화). A21·리스크·§4-C 갱신.
4. §0-VOID → README §남은결정(996-1001)·§수용기준 전체·token URL(referrer 포함)·status 필드 무효 명시.
5. 필드명 → README 정합 `childProcessGroupId`(값만 OS별: POSIX pgid number / Windows pid marker string).
6. `ps lstart`·CreationDate → 불투명 문자열 정확일치(파싱 금지).
7. usage → nullable + `.partial()`(러너 생략 허용).

## 4. 외부 리뷰 반영
- raw: `_workspace/reviews/r4c_codex.md`·`r4c_agy.md`(휘발).
- <details><summary>결정적</summary>codex: "Zod not executable; A9 M4/M5 violation; Windows Job Object shell-only unbuildable(P/Invoke=native); README 남은결정 not voided." agy: "Zod invalid pseudo-code; TerminateJobObject cannot run via standard PowerShell cmdlets."</details>

## 5. 다음 단계 참조
- **다음 착수:** r3 → codex+agy **R5 재인증**(실 Zod 유효성·A9 1:1·taskkill shell 실현가능·void 완결 확인). 수렴 시 **CERTIFIED**·M1 착수.
- **핵심 결정:** Windows v0.5 = taskkill(shell), Job Object v0.6 native — "shell 확정" 정직화. Zod가 schema-valid 유일 정의. 필드명 README 정합.
- **주의:** R1→R4 매 라운드 실결함 발견(단 R4는 전부 기계적). r3은 자체검증 가능(Zod 문법·A9 분리·taskkill 셸빌드) 하나 외부 R5로 확정 권장.
- **수렴 추세:** 구조(R1/R2)→통합갭(R3)→기계적(R4). 아키텍처 안정, 남은 건 표현 정확도. R5 통과 시 빌드 착수 가능 판단.
