# M1 결과서: 기반 구현 (스캐폴딩·read API·runs reader)

- 대상: `harness-ui/`(신규 Node 앱) · 브랜치 `feat/harness-ui-v0.5`
- 일자: 2026-07-09
- DoD: A1·A3api·A5be. 외부감사: codex+agy 4라운드 → 고위험 0.

## 1. 작업 요약
- Fastify+Zod+tsx+vitest 스캐폴딩. schema 모듈(§5 Zod)·state_home·exec(no-shell)·paths(SAFE_SEGMENT).
- 어댑터: runtime detect·harness inventory(agents/skills 파서)·runs reader(스트리밍).
- read-only API: /api/{runtimes,harness,agents[/:name],skills[/:name],runs[/:id][/events][/agents],health}. 최소 web(vite proxy).
- 테스트 15개(schema·runs·api). typecheck clean. 서버 부팅+실데이터 확인(claude agents:4 skills:6).

## 2. 검증 결과
- typecheck PASS · vitest 15/15 PASS.
- 런타임 스모크: /api/harness 실데이터·/api/runs empty-safe(A5be)·A3api counts.
- e2e(하네스 기준): 이 레포(myHarness 팩토리)를 projectRoot로 실제 인벤토리 반환.

## 3. 외부감사 (4R, 고위험 0까지)
- R1: agy 3 high(events 무제한 read·cursor off-by-one·frontmatter BOM/멀티라인). codex med.
- R2: base symlink re-anchor·leaf symlink 탈출·stream crash·FD leak·"Note:" 오분할(high).
- R3: safeFile check-reopen TOCTOU(codex high). agy 일회성 거부.
- R4: **양 리뷰어 "M1 no high-risk"** (low만: 부모 swap·Windows O_NOFOLLOW=위협모델 수용, isWithinRoot `..b`=수정).
- 수정: 스트리밍(readLines)·after -1·frontmatter 들여쓰기 규칙·O_NOFOLLOW+fstat+동일 handle(TOCTOU 제거)·base realpath 앵커·isWithinRoot 세그먼트.

## 4. 다음 단계 참조
- **다음: M2 Supervisor 코어** — spawn(로그파일·fd close)·서명 owner 레지스트리(§4-A)·HMAC 키·heartbeat·구조화로그 커서·events 멱등·원자쓰기·mock runner. DoD A6be·A24·A25.
- **핵심 결정:** 파일 read는 O_NOFOLLOW+fstat+동일 handle(재open 금지). frontmatter 연속행=들여쓰기만. runId=SAFE_SEGMENT+symlink 거부+realpath 경계.
- **주의:** 로컬 개발도구 위협모델(공격자 로컬 FS write=이미 침해) — 부모디렉토리 swap·Windows O_NOFOLLOW은 수용된 low. M2 supervisor는 이 레지스트리/경로 규율 재사용.
