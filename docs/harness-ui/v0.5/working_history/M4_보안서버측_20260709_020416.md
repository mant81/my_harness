# M4 결과서: 화면+보안 (서버측·보안 — React 화면 UI 잔여)

- 대상: harness-ui/src/server/{security.ts,api/index.ts,adapters/{drift,statestats}.ts,index.ts}
- 일자: 2026-07-09 · DoD(서버측): A7·A8·A10-A14·A23·A26·A27·A28·A29·A35-A40·A44·A45 · 외부감사 codex+agy 5R → 고위험 0
- 잔여: React 6화면(A2·A3·A4·A9a 표시 — 저위험 프레젠테이션, 별도 UI 작업)

## 1. 작업 요약
- 보안 미들웨어: bootstrap→session 교환(single-use·fragment·쿼리토큰 금지)·Host allowlist(127.0.0.1/localhost/[::1])·Origin(state-mutating)·nosniff/referrer·denylist.
- artifact 서빙: O_NOFOLLOW open·fstat·realpath(projectRoot 앵커)·dev/ino 바인딩·크기상한·attachment/nosniff.
- 어댑터: drift(A4/A4b·sync-plan 무변경)·state-stats(A35-38 정적·TS 네이티브·bounded)·settings(read-only)·ops status(A7/A8).
- 테스트 51개(보안 게이트·auth-bypass 회귀·artifact·drift·state-stats).

## 2. 검증
- typecheck PASS · vitest 51/51 PASS. auth-bypass 회귀 실증(인코딩·authority-form·//·backslash·dot-seg·OPTIONS/HEAD → 전부 non-200 unauth).

## 3. 외부감사 5R (고위험 0까지 · verify가 핵심)
- R1: artifact symlink-root 탈출·size-cap TOCTOU·statestats symlink/무제한.
- R2: artifact 부모 symlink TOCTOU·statestats 무제한 read.
- R3: **URL-encoded auth 우회(`/%61pi/`→200 unauth)** — agy 발견·codex 오판. **inject 실증으로 확인**(맹신 회피가 정반대로 판명 — agy 정답).
- R4: **authority-form 우회(`http://host/api/`)** — 양 리뷰어 수렴, 실증 확인.
- R5: codex no-high + **추가 벡터 9종 실증 전부 gated**.
- 수정: 게이트를 `new URL().pathname`+decode 정규화(라우터 정합)·artifact dev/ino·statestats bounded fd.

## 4. 다음 단계 참조
- **잔여 M4 UI:** React 6화면(DESIGN.md Linear 토큰)·3-state·위험작업 확인·키보드 nav — 저위험, 서버 API 위에 구축.
- **다음: M5 실행 인증** — POST /api/runs(dryRun)·cancel 엔드포인트(M3 reconcile 연결)·실 codex/claude 스모크(opt-in)·Task:\n prefix·`--` positional·`--allowedTools` 배열. DoD A6·A9b·A15·A16·A17.
- **핵심 교훈:** auth 게이트는 **라우터 정규화와 동일 규칙**(pathname+decode)이어야 — 리뷰어 의견 갈릴 때 **inject 실증**이 진실. artifact/파일 read 는 O_NOFOLLOW+fstat+inode 바인딩 패턴 재사용.
