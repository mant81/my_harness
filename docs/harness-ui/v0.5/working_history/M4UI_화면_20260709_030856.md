# M4-UI 결과서: React 8화면 프론트엔드

- 대상: harness-ui/src/web/{main.tsx,App.tsx,api.ts,ui.tsx,screens.tsx,index.html,styles.css} + vite react 플러그인·tsconfig jsx
- 일자: 2026-07-09 · DoD A2·A3·A4·A5·A7·A8·A9a·A35-A38·A43 화면 배선 · 외부감사 codex+agy → 고위험 0

## 1. 작업 요약
- **8화면 IA**(§설계 사이드바: Overview·Build·Agents·Skills·Runs·Drift·Ops·Settings). 랜딩 없이 첫 화면=Overview 대시보드. hash 라우팅(외부 router 무의존).
- **Overview**: 런타임(A2)·인벤토리(A3)·구성건강도(A35 heuristic)·D4규율(A36)·업데이트(A37)·진화이력(A38).
- **Build**: dry-run 폼 골격(A9a) + 실행(POST /api/runs, dryRun 기본 on).
- **Agents/Skills**: 목록+상세(A43 triggers). **Runs**: 목록·상세·이벤트·agent status·artifact(A5·A6). **Drift**: findings + sync-plan 미리보기(무변경). **Ops**: 런타임 상태(A7·A8). **Settings**: 조회 전용.
- **보안**: 세션토큰 Bearer 헤더만(쿼리·로그·DOM 미노출), fragment strip-first(api.ts), 전 텍스트 React escape(innerHTML/eval 0), fetch 경로 encodeURIComponent, mutating same-origin(서버 Origin/Host/token 재검증).
- React 18 + @vitejs/plugin-react. 빌드 154KB(gzip 50KB).

## 2. 검증
- typecheck PASS · vite build PASS(34 모듈) · 기존 유닛 77/77 회귀 PASS.
- **e2e**(test/e2e.mjs): 현 하네스 기준 실 HTTP 부팅·auth 교환·실 inventory(에이전트4·스킬6)·drift(10)·state-stats·dry-run·flag injection 차단·cross-origin 차단 17/17 PASS.

## 3. 외부감사 (고위험 0까지)
- R1: **codex no high-risk**(XSS/토큰/경로 sink 없음 확인). agy 보안감사 거부(무판정).
- R2: agy(코드리뷰 프레이밍) HIGH 1건 — `useApi` path 변경 시 이전 data 미클리어 → stale 렌더(run 전환 시 이전 데이터 순간 노출). codex context-mode MCP 취소로 blocked(무판정).
- **수정**: `useApi` 이펙트에 `setData(null)` 추가 + `RunDetail key={runId}` 완전 remount.
- R3: **agy no high-risk**(수정 확정 + XSS/토큰/경로/훅/rejection 전 항목 clear).
- R4: **codex no high-risk**(context-mode 우회·직접 read).
- 판정: 양 엔진 clear(codex R1·R4, agy R3). codex R2/R3 blocked=자체 MCP 툴 결함(무판정, 재실행서 해소).

## 4. 다음 단계 참조
- **v0.5 전 마일스톤(M1-M6) + e2e 완료.** 프론트+백+supervisor+런처+보안 종단 작동.
- **핵심 결정:** 설계의 React/Vite 준수(vanilla 대체 안 함). 8화면 = §IA 사이드바 그대로. 프론트는 presentation(경화된 API 소비) → 위협면 XSS·토큰만, React 기본 escape로 해소.
- **주의(v0.6):** 실행파생 통계·관측성 대시보드·dead-root(A21b)·네이티브 identity. 3-OS CI 는 GitHub Actions(`.github/workflows/ci.yml`) — 로컬 macOS만 검증됨.
