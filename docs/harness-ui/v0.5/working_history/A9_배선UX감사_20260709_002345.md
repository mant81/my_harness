# A9 작업결과서: 배선(wiring) + UX 재검토 + 외부감사

- 대상: `wiring-ux-review.md` + 정본 §API/수용/UI
- 일자: 2026-07-09
- 리뷰: codex + agy, 러너 claude 제외

## 1. 작업 요약
- 화면↔API↔소스 배선 매트릭스 + UX 재검토 문서 작성 → 외부감사 → 내 리뷰의 헐루시 1건·놓친 배선/UX 다수 수정. 정본에 A39-A46 반영.

## 2. 판정
내 리뷰 정확도: **W1(Settings)·W2(stream)·W3(Ops import)·W5(agent 선언스킬) 확인** / **W4 = 오류(헐루시)**.
- **W4 헐루시(codex·agy):** `/api/overview/state-stats`를 todo/리뷰에 썼으나 설계 §API 미정의 → A35-A38 API 계약 부재. **외부감사가 내 헐루시 포착.**
- **놓친 배선(감사):** Build "리소스제한"↔payload 없음 · Runs "재시도기록"↔API 없음 · Skills "트리거"↔필드 없음 · Agents "최근상태"↔run-state join · bootstrap 교환 엔드포인트 없음 · drift sync-plan 응답 스키마 없음 · artifact list/download 스키마.
- **놓친 UX:** cancel race(멱등/409) · action 에러 토스트/retry · queue/cap·polling interval · 키보드 nav. **정정:** 내 U3(모든 ops dry-run) 과장 — dry-run은 run/drift-apply만.

## 3. 반영 (A39-A46 신설)
- A39 `GET /api/settings`(read-only) · A40 `GET /api/overview/state-stats`(W4 수정) · A41 bootstrap 교환 · A42 Build payload 필드 · A43 Skills triggers·Agents join·Runs retry · A44 drift-plan 스키마 · A45 artifact UX · A46 UX 표준(3-state·토스트·cancel 멱등·polling·키보드·접근성·모바일·위험작업 확인).
- 정본 §6·M4 DoD·헤더(R8 범위) + wiring-ux-review(W4 정정·W6-8·U7-10) + todo M4·main_todolist 동기.

## 4. 외부 리뷰 반영
- raw: `_workspace/reviews/wire_{codex,agy}.md`(휘발).
- <details><summary>결정적</summary>codex: "review missed /api/overview/state-stats not in spec — wiring gap; Build resource limits, Skills triggers, Agents recent status, drift plan schema, bootstrap API all missing." agy: "W4 hallucinated an API definition; missed resource limits, retry, action-error toast, download button, keyboard nav."</details>

## 5. 다음 단계 참조
- **다음:** 배선/UX 델타(A39-A46) + 런처(A30-34) + 상태통계(A35-38) → **R8 통합 재인증**. 통과 시 전체 CERTIFIED.
- **핵심 교훈:** 내 리뷰가 엔드포인트를 헐루시(todo에 collector 썼으나 §API 미정의) — 외부감사가 잡음(맹신 회피 가치 실증). 화면 IA의 모든 필드는 API 소스와 1:1 배선 확인 필수.
- **주의:** A42(리소스제한)·A43(retry/triggers) 일부는 IA서 제거 vs API 추가 결정 필요(구현 시). Settings·state-stats·bootstrap은 read-only/교환으로 v0.5 수정 API 비활성 원칙 유지.
