# M6 결과서: 런처(설치 자동화)

- 대상: harness-ui/src/server/{launcher.ts, security.ts(rotate·bootstrap 파일), api(/healthz·artifact walk), web/main.ts(fragment strip)}
- 일자: 2026-07-09 · DoD A30·A30w·A31·A32·A33·A34 · 외부감사 codex+agy 5R → 고위험 0

## 1. 작업 요약
- planLaunch 오케스트레이터: 멱등(serverAlive→새 토큰 재오픈)·node/npm 해소(graceful)·의존성 없으면 **동의 게이트**(silent 금지)·lockfile 해시 fail-closed·`npm ci --ignore-scripts`(RCE 차단) 계획·OS 브라우저(execFile argv, Windows cmd /d /s /c start).
- 토큰: fragment(#)만(쿼리/로그 미노출)·client strip-first(fetch 전 동기)·`<state_home>/.bootstrap` 0600(chmod-first·O_NOFOLLOW)·rotate write-then-publish.
- artifact 서빙: 전 경로 컴포넌트 symlink walk 거부(base 포함)·leaf O_NOFOLLOW·이중 realpath 앵커(선계산)·dev/ino 바인딩·크기상한.
- 테스트 77개.

## 2. 검증
- typecheck PASS · vitest 77/77 PASS: planLaunch(needs-consent·lockfile-untrusted·install-and-open)·verifyLockfile fail-closed·openArgs per-OS·fragment URL·writeBootstrap 0600(기존 0644도 강제) 실증.

## 3. 외부감사 5R (고위험 0까지)
- R1: launch() 오케스트레이터 부재·verifyLockfile fail-open.
- R2: fragment async 중 URL 잔존·writeBootstrap chmod-after-write·artifact 부모 symlink TOCTOU.
- R3: **artifact base 컴포넌트(artifacts/runId) symlink 미검**(codex 재시도서 포착) → in-project 노출.
- R4: realBase 앵커가 walk 후 계산(swap 창) → 선계산으로 이동.
- R5: **codex no high-risk**(agy R5 거부=무판정). R1-R4 실수정+실증.
- 핵심: 런처는 결정·게이트 순수함수(planLaunch)로 테스트가능. 실 npm/브라우저 spawn 은 호출측 execFile.

## 4. 다음 단계 참조
- **다음: e2e** — 현 하네스(myHarness 레포) 기준 종단: 서버 부팅·auth 교환·실 inventory/drift/state-stats·dry-run + **실 codex 스모크(A17)** 로 supervisor 파이프 종단 검증.
- **핵심 결정:** 설치 자동화=첫 실행 bootstrap(plugin-install-time 불가). npm ci --ignore-scripts+lock 해시+동의 3중. 토큰 fragment+strip-first. artifact 전경로 symlink 거부.
- **주의:** M4 React 화면 UI 는 저위험 잔여(별도). 6 마일스톤 코어+보안 완료.
