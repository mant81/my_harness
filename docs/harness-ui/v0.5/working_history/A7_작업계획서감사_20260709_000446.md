# A7 작업결과서: 단계별 작업계획서(todo) 작성 + 외부감사 + 보강

- 대상: `docs/harness-ui/v0.5/todo/` (main + M1~M6)
- 일자: 2026-07-09
- 리뷰: codex + agy, 러너 claude 제외
- 근거: 인증된 설계 `design-v0.5-final.md`(코어 R5 CERTIFIED)

## 1. 작업 요약
- 마스터(main_todolist) + 마일스톤별(M1~M6) 작업계획서 7종 작성 — 체크리스트·수용기준(A1-A34)·DoD·게이트·테스트/fixture·리스크 배선.
- 외부감사 1R → **COVERAGE 완전**(누락 수용기준 0·잘못된 마일스톤 0) / **NOT ready**(태스크 세분화 갭). fix 적용.

## 2. 감사 판정
- **COVERAGE: complete** (양 리뷰어) — A1-A34 전부 마일스톤 DoD에 존재, 설계 §7 매핑과 일치.
- **ORDERING: issue** — 3-OS CI 매트릭스가 M3에 있는데 M2 게이트가 요구(순서 위반, blocking).
- **Blocking:** ①CI 매트릭스 순서 ②M2에 status.json/agents.json 로그파싱 갱신 태스크 누락(A6be 미충족).
- **High:** `command -v`=shell(§execFile 위반) · Host allowlist 드리프트+dev-proxy 누락 · artifact 경화 불완전(lstat·O_NOFOLLOW·S_ISREG·크기) · token 엔트로피/perms/로그 테스트 · 레지스트리 rollback 태스크 · HMAC 키관리 태스크 · DESIGN.md CSS 토큰 태스크.
- **Med:** schema Usage/ISO · `--allowedTools` argv 테스트 · pagination shape · package root/lock 심링크.

## 3. 보강 (적용)
- main/M1: **3-OS CI 워크플로 M1 생성**(M2 전제) + HMAC 세션키 수명주기.
- M1: `command -v`→`which/where.exe`(shell 없음) · schema에 Usage/ISO+스냅샷 테스트.
- M2: **status.json 동적 갱신·agents.json 작성 태스크**(A6be) + 레지스트리 손상 rollback + HMAC 키 선행.
- M3: reconcile 중 레지스트리 rollback(stale 태깅).
- M4: DESIGN.md CSS 토큰 태스크 · Host 모드별 정확+dev-proxy trust-proxy · artifact 경화(lstat/O_NOFOLLOW/S_ISREG/크기) · token CSPRNG≥128bit·저장 perms·로그 음성 테스트.
- M5: `--allowedTools` argv 스냅샷 테스트(콤마·`--tools` 거부).
- M6: package root·lock 심링크 거부.

## 4. 외부 리뷰 반영
- raw: `_workspace/reviews/todo_codex.md`·`todo_agy.md`(휘발).
- <details><summary>결정적</summary>codex: "COVERAGE complete; CI matrix ordered after milestone that requires it(blocking); command -v implies shell." agy: "Missing tasks for status.json/agents.json updates required for M2 DoD; CI ordering violation."</details>

## 5. 다음 단계 참조
- **다음:** 보강된 todo → (선택) 재감사 R2로 태스크 갭 수렴 확인 → 구현 M1 착수.
- **핵심 결정:** CI 매트릭스 M1 선생성(모든 게이트 전제). supervisor가 status/agents 저자(로그 파싱 갱신 태스크 필수). 레지스트리 rollback은 M2/M3 실태스크.
- **주의:** 런처(M6)는 설계 R7 인증 후. M1-M5는 착수 가능. COVERAGE는 완전하나 태스크 실행성은 R2 재감사로 확정 권장.
