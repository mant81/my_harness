# M2 결과서: Supervisor 코어

- 대상: harness-ui/src/server/{supervisor,lib} · 브랜치 feat/harness-ui-v0.5
- 일자: 2026-07-09 · DoD: A6be·A24·A25 · 외부감사 codex+agy 6R → 고위험 0

## 1. 작업 요약
- schema 최종 저자=supervisor. manifest/status(queued)·spawn(로그파일 stdio·fd close·detached·env allowlist)·서명 owner 레지스트리(HMAC·O_EXCL·<state_home>)·HMAC 키 수명주기.
- ingest: offset read(전체 재읽기 금지)·구조화로그 파싱→events append(멱등·durableMaxSeq dedup)·status/agents baseline 갱신·원자쓰기·torn-tail repair·oversized 라인 skip·런별 뮤텍스.
- mock runner(raw-only). 테스트 26개.

## 2. 검증
- typecheck PASS · vitest 26/26 PASS(A6be schema-valid 생성·A24 append 무재작성·A25 크래시 dedup·baseline 유지·torn repair·agent traversal 차단·spawn e2e).

## 3. 외부감사 6R (고위험 0까지)
- R1: events 무제한 read·cursor off-by-one·frontmatter(→M1 잔재 아님, M2 신규)·… agy 3 high.
- R2: status/agent 배치 regress·크래시 중복·env leak·HMAC 재로드·registry O_EXCL·nonce.
- R3: torn-tail append 손상·projection skip 유실·env PATHEXT.
- R4: repair >64K·oversized stall / durableMaxSeq O(N²)·**raw.agent path-traversal 임의쓰기**.
- R5: durableMaxSeq >64K -1·spawn error crash.
- R6: **양 리뷰어 no high-risk.**
- 매 라운드 실결함(맹신 없이 실코드/테스트 대조) → 수정 → 회귀 테스트.

## 4. 다음 단계 참조
- **다음: M3 OS 어댑터** — POSIX pgroup + Windows detached pid-tree(taskkill)·identity(shell·불투명 startTime)·reconcile 3중검증(서명+identity+exe/cwd)·3-OS CI. DoD A18·A19·A20·A21·A21b·A22.
- **핵심 결정:** ingest 멱등=durableMaxSeq(events 권위)+seq skip, projection은 skip 전 수행. 파일명(agent 등)은 allowlist 후 사용. spawn env allowlist+child.on(error). 레지스트리 서명=kill 3중검증의 '서명' 축(M3 reconcile이 identity·exe/cwd 추가).
- **주의:** M3 reconcile은 이 서명 레지스트리 + identity(pid+불투명 startTime) 대조로 kill 판정(단일근거 금지). Windows=taskkill /T(Job Object는 v0.6).
