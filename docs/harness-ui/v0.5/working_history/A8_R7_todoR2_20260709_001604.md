# A8 작업결과서: R7(런처+상태통계 델타) + todo-R2 재감사 + 경화

- 대상: `design-v0.5-final.md`(런처 A30-34·상태통계 A35-38) + `todo/`(보강본)
- 일자: 2026-07-09
- 리뷰: codex + agy ×2 태스크(4 프로세스 병렬), 러너 claude 제외

## 1. 작업 요약
- R7(델타 재인증) + todo-R2(작업계획서 재감사) 병렬 → 둘 다 **NOT certified**, 동일 2근원 수렴(신규 A35-A38 실현가능성 + 런처 A34 토큰전달). 경화 적용.

## 2. 판정 (수렴)
**Blocking:**
1. A34 토큰 전달 미정의(R7 codex·agy) — URL/로그 금지인데 브라우저 수신법 없음 → **URL fragment**(`#`, 서버 미전송) + client 교환·strip.
2. A37 factory-drift 정적 불가(R7×2·todo2×2) — 빌드 하네스에 팩토리 경로 부재 → 로컬 manifest(measured) / factory-drift(경로 시만·else unknown) 분리.
3. A36 check-artifacts 셸아웃 의존(R7 agy·todo2×2) — 비정적·스크립트 부재 → **TS 네이티브** 검사.
4. (todo) A35-A38 백엔드 collector·fixture·테스트 태스크 부재(UI 라벨만) — todo2 codex.

**High/Med:** A35 coverage=heuristic(선언 파싱, measured 아님) · main_todolist M4에 A35-A38 누락 · A30w `cmd.exe /d /s /c` 정합 · `[::1]:{port}` 괄호 · A35/A38 AGENTS.md도 파싱(듀얼) · 토큰 경화 테스트 구체화 · §8 계층A=v0.5 명확화.

## 3. 경화 (적용)
설계: A34 fragment 전달(strip) · A30w `cmd.exe /d /s /c` · A35 coverage=heuristic·AGENTS.md 추가 · A36 TS 네이티브(셸 금지) · A37 로컬 manifest/factory-drift 분리(unknown) · observability §7b feasibility·§8 계층A=v0.5.
todo: main_todolist M4 A35-A38 추가 · M4 백엔드 collector(`/api/overview/state-stats`)+fixture+테스트 · A36 네이티브·A37 manifest 폴백·`[::1]` 괄호·토큰 경화 테스트.

## 4. 외부 리뷰 반영
- raw: `_workspace/reviews/{r7,todo2}_{codex,agy}.md`(휘발).
- <details><summary>결정적</summary>R7 agy: "browser bootstrap token chicken-and-egg → URL fragment"; R7 codex: "factory drift assumes factory path — split local/factory". todo2 codex: "A35-A38 no backend collector/fixture — DoD can pass by rendering text"; todo2 agy: "A36/A37 factory-skill deps in built harness".</details>

## 5. 다음 단계 참조
- **다음:** 경화된 델타(런처+상태통계) + todo → 최종 재인증(R8/todo-R3) 또는 수용. 통과 시 전체 CERTIFIED.
- **핵심 결정:** 토큰=URL fragment(서버/로그 미노출). 상태통계는 계층A(정적·v0.5)만 정본, factory-drift·check-artifacts 셸은 제거(빌드 하네스 자립). A35 coverage는 heuristic 라벨.
- **주의:** 신규 상태통계 지표는 빌드된 하네스 환경 자립성이 관건(팩토리 스크립트/경로 의존 금지). 백엔드 collector 없이 UI 라벨만이면 DoD 무의미(fixture+테스트 필수).
