# A2 작업결과서: v0.5.1 개정안 재감사(R2) + Windows 결정 + v0.5.2

- 대상: `design-revision-v0.5.1.md` (v0.5 개정 초안)
- 일자: 2026-07-08
- 리뷰: codex + agy(Gemini 3.1 Pro High), 러너 claude 제외
- 선행: `A1_설계감사_20260708_224531.md`

## 1. 작업 요약
- v0.5.1 개정안 재감사(R2) → **미수렴**(MUST-FIX 다수 잔존/신규).
- 잔존 대부분이 "Windows 실행 지원" 결정에 수렴 → 사용자 결정: **Windows 완전 지원(Job Object 어댑터)**.
- 결정 반영 v0.5.2 개정안 작성: `design-revision-v0.5.2.md`.

## 2. 재감사 판정 (R2)
R1 PARTIAL · **R2 NOT-RESOLVED** · R3 PARTIAL · R4 RESOLVED(agy)/PARTIAL(codex) · R5 PARTIAL · R6 PARTIAL.

확인(양 리뷰어 수렴):
1. **owner.json 스푸핑**(codex#1·agy#1) — v0.5.1이 넣은 신규 구멍. `_workspace` writable → groupId 위조 → reconcile 시 무관 프로세스 kill.
2. **startTime Node 미제공**(codex#2·agy#2) — identity(pid) 이식성 미명세.
3. **Windows Job Object ↔ R3 생존 모순**(codex#3·agy#3) — 핸들 닫힘=child 종료면 orphan 없음.
4. **raw-log tail 취약 + offset 미영속**(codex#4·agy#4) — 코어 승격 필요, 구조화 JSON 소비.
5. **0600 Windows 무효**(codex#9·agy#5) — Node mode≠ACL.
6. 토큰파일 artifact API 미차단(codex#8) · mock 스키마 직접방출=우회(codex#12) · snapshot 불변 optional(codex#11) · fd leak(codex#13) · README 수용기준 HARNESS_RUN_ID 잔존(codex#7).

정제(충돌 판정): 심링크 — artifact 서빙=거부(codex#10), 인벤토리 read=realpath-경계(agy#6). 둘 다 부분정답 → 경계 분리.

## 3. 제품 결정
**Windows 실행 완전 지원** 선택(사용자). → v0.5.2가 Job Object 수명·재획득·ACL·per-OS identity 전부 명세. (대안 "macOS/Linux만" "실행 v0.6" 기각.)

## 4. v0.5.2 반영 요지
- R2-A: 서명 owner 레지스트리 `_workspace` 밖(0700/ACL)+`O_EXCL`, 3중 검증(서명·identity·exe/cwd) 통과분만 kill.
- R2-B: identity() OS별(Linux `/proc`·macOS `ps`/native·Windows CreationDate), pid+startTime로 PID reuse 판정.
- R2-C: Windows named Job Object(KILL_ON_JOB_CLOSE 미설정 → child 생존), reconcile = job 재-open → 실패 시 레지스트리 fallback.
- R1/R3: 구조화 JSON 로그 + 영속 커서 + 멱등 승격(코어), supervisor 최종 저자.
- R4: 수용기준 매트릭스(criterion→step→version→OS), README HARNESS_RUN_ID 항목 삭제·레지스트리 기준 교체.
- R5: single-use bootstrap→session 교환(이식성), 토큰파일은 ACL.
- R6: artifact 서빙=심링크 거부+불변 스냅샷 의무, 인벤토리=realpath 경계.

## 5. 외부 리뷰 반영
- raw: `_workspace/reviews/rev_codex.md`·`rev_agy.md`(휘발).
- <details><summary>결정적</summary>codex: "owner.json in writable _workspace does not prove ownership; Job Object KILL_ON_JOB_CLOSE contradicts survival; acceptance still requires HARNESS_RUN_ID." agy: "owner.json spoofable→kill arbitrary process; startTime not native in Node; 0600 no effect on Windows."</details>

## 6. 다음 단계 참조
- **다음 착수:** v0.5.2(`design-revision-v0.5.2.md`)를 codex+agy **재감사(R3)** — Windows Job Object 재획득·서명 레지스트리·per-OS identity가 실제 수렴하는지 검증.
- **핵심 결정과 이유:** Windows 완전지원 확정 → supervision 복잡도 수용. kill 3중 검증(서명+identity+exe/cwd)은 무관 프로세스 종료 방지의 핵심 — 절대 단일근거 kill 금지.
- **주의:** v0.5.2 미검증 초안. R3 통과 전 구현 착수 금지. native addon vs shell(identity) + `<state_home>` 경로 미확정.
