# A3 작업결과서: 통합 정본 R3 인증 감사

- 대상: `design-v0.5-final.md` (4문서 통합 정본)
- 일자: 2026-07-08
- 리뷰: codex + agy(Gemini 3.1 Pro High), 러너 claude 제외
- 선행: A1(R1)·A2(R2+Windows 결정)

## 1. 작업 요약
- 4문서(README·v0.5.1·v0.5.2·observability) → 단일 정본 `design-v0.5-final.md` 통합 + 성공률 장치(수용 매트릭스·마일스톤 DoD·리스크 레지스터).
- R3 인증 감사 → **NOT certified.** 두 리뷰어 5 blocking 수렴.

## 2. 판정 — 인증 실패, blocking 6 + high
매트릭스 GAPS · README-consistency AMBIGUOUS · v0.5.2 absorption PARTIAL(양 리뷰어 일치).

**Blocking:**
1. README "override 외 유효" 모순 (codex#3·agy#5) — README §상태스키마 227-246(HARNESS_RUN_ID/environ reconcile)·§실행전략 760/799·token URL이 명시 무효화 안 됨 → frozen README서 삭제된 로직 재유입.
2. `<state_home>` 미정 (codex#5·agy#1) — 레지스트리/토큰 보안경계 경로 없음 → M2/M4 불가.
3. "schema-valid"/event 스키마 미정 (codex#2·agy#2,8) — 정확 Zod 없음, agent/skill/usage TBD → M2/M5 DoD 검증 불가.
4. native-vs-shell 미결 (agy#3) — M3 지연.
5. 마일스톤↔step 매핑 모순 (codex#1,9·agy#4) — A2/A4 M1&M4, A9 step9인데 M1, A25 step7인데 M2, A16 step6인데 M5. 스프린트 계획 불가.
6. token bootstrap ↔ README 모순 (codex#4).

**High:** 매트릭스 positive 갭(artifact 서빙·snapshot import·drift sync-plan/full — codex#6,7,8·agy#6) · v0.5.2 SHOULD 소실(atomic fsync·pagination·`--allowedTools` 배열·drift 스키마 — agy#7·codex#10) · 리스크에 CI 매트릭스·fixture·rollback·schema-valid 정의 없음(codex#10·agy#9).

## 3. 판정 근거 (맹신 회피)
- 정본 §9에 실제로 `<state_home>`·event 필드·native/shell "미확정" 존재 확인 → blocking 타당.
- 정본 preface "README override 외 유효" + README 227-246 HARNESS_RUN_ID 잔존 확인 → 모순 타당.
- §6 매트릭스 step 다중나열 vs §7 DoD 단일 → 매핑 모순 타당.
- v0.5.2 SHOULD(fsync·pagination·배열)가 정본에 없음 확인 → 소실 타당.

## 4. 후속 — 정본 개정 r2 (fix 계획)
1. **README 명시 무효화:** preface에 "README §상태스키마 227-246·§실행전략 760/799·token URL 무효, §4-C/§5로 대체" 블록.
2. **`<state_home>` 확정:** macOS `~/Library/Application Support/harness-ui`, Linux `${XDG_STATE_HOME:-~/.local/state}/harness-ui`, Windows `%LOCALAPPDATA%\harness-ui`. 퍼미션/ACL 명시.
3. **스키마 published:** manifest/status/events/agents Zod 스키마 인라인 + schemaVersion·종료타임스탬프·cancelRequestedAt·exitSignal·stateReason·supervisorVersion. "schema-valid"=Zod parse 통과. event `agent`·`skill`·`usage`는 **v0.5 선반영**(v0.6 관측성 선행).
4. **native-vs-shell:** v0.5 = **shell 확정**(ps/PowerShell), native는 v0.6 최적화.
5. **매트릭스↔마일스톤 재정렬:** 각 수용기준을 완전충족 마일스톤 하나에 배정.
6. **매트릭스 positive 행 추가:** artifact 성공서빙·snapshot import(raw보존·normalized·unparsed)·drift sync-plan(무변경)·full drift(CLAUDE↔AGENTS·skills).
7. **v0.5.2 SHOULD 복원:** atomic temp+fsync+rename·pagination `{items,nextAfter,hasMore}`·`--allowedTools` 배열·`DriftFinding` 스키마.
8. **리스크 보강:** 3-OS CI 매트릭스·fixture 스위트·rollback(레지스트리 손상)·schema-valid 정의.

## 5. 외부 리뷰 반영
- raw: `_workspace/reviews/final_codex.md`·`final_agy.md`(휘발).
- <details><summary>결정적</summary>codex: "README remains valid-except-overridden but still specifies HARNESS_RUN_ID reconcile at 239-242,983,986-987; schema-valid undefined; state_home undefined." agy: "milestone DoD contradicts step numbers—sprint planning impossible; lost v0.5.2 atomic-write/pagination/allowedTools." </details>

## 6. 다음 단계 참조
- **다음 착수:** 정본 개정 r2(§4 8건 fix) → codex+agy R4 재인증 → 통과 시 v0.5 확정·구현 착수(M1).
- **핵심 결정:** v0.5 = shell identity 확정 / event agent·skill·usage 필드 v0.5 선반영 / `<state_home>` OS 표준경로 / README 관련 섹션 명시 무효화(포인터 애매성 제거).
- **주의:** "frozen README except overridden"은 위험 — dev가 무효 섹션을 읽음. r2는 무효 목록을 명시 열거해야. schema-valid는 published Zod로만 정의(구두 금지).
- **성공률 관점:** R3가 잡은 핵심 = 매트릭스↔마일스톤 정합·schema published·README 애매성 제거. 이 3개가 스프린트 착수 가능성의 게이트.
