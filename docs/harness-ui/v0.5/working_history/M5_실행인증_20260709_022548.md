# M5 결과서: 실행 인증 (실행 API·supervision·CLI 계약)

- 대상: harness-ui/src/server/{exec-run.ts, supervisor/supervisor.ts(superviseRun/finalize), api POST /api/runs·cancel}
- 일자: 2026-07-09 · DoD A6·A9b·A15·A16 (A17 실 스모크=e2e 단계) · 외부감사 codex+agy 5R → 고위험 0

## 1. 작업 요약
- RunRequest Zod(runtime·mode·domain·permissionMode allowlist·model/allowedTools no-leading-dash·targets allowlist·dryRun). buildArgv: codex `exec --json --sandbox -o -- Task:\n`·claude `-p --output-format stream-json --allowedTools(배열) -- Task:\n`. safePrompt `Task:\n` prefix.
- launchRun: dry-run(파일 미기록 미리보기) / 실행(manifest+superviseRun). superviseRun: spawn+주기 ingest(500ms)+exited(동기캡처·exit/error)→finalize(최종 ingest·terminal status·owner 정리). 감독 부착 finally 보장.
- POST /api/runs·/api/runs/:id/cancel(reconcile 3중검증·terminal 멱등). 테스트 67개.

## 2. 검증
- typecheck PASS · vitest 67/67 PASS: argv 계약(A16 `--` positional·A15 metachar 미분해)·injection Zod 거부·dry-run 무기록(A9b)·superviseRun 자동 finalize(A6, 수동 ingest 없이 completed)·cancel.

## 3. 외부감사 5R (고위험 0까지)
- R1: 프로덕션 supervision 부재·spawn 실패 삼킴·model/allowedTools flag injection·cancel terminal·agents/ 미생성.
- R2: early-exit 유실·identity-fail 좀비·rejection 미처리·cancel TOCTOU·status RMW.
- R3: exited error-only 미해결·post-spawn reject 좀비.
- R4: superviseRun status-write가 finalize 부착 전 throw→미감독.
- R5: **양 리뷰어 no high-risk.**
- 수정: superviseRun(exited 동기캡처·finally 감독부착·running-write 순서)·flag injection 정규식·cancel 멱등+TOCTOU 재검·spawn 실패 kill.

## 4. 다음 단계 참조
- **다음: M6 런처** — 첫 실행 bootstrap(node/npm 확인·동의 `npm ci --ignore-scripts`·§4-A 멱등·fragment 토큰·OS별 브라우저). DoD A30-A34. **그리고 e2e** — 현 하네스 기준 실 codex 스모크(A17)로 종단 검증.
- **핵심 결정:** 실행 감독은 superviseRun(exited 프로미스 동기캡처·finally 부착·finalize 멱등). CLI 계약 argv-only(shell 금지)·Task:\n·`--` positional. model/allowedTools no-leading-dash.
- **주의:** 실 codex/claude 스모크(A17)는 모델 호출 비용 → opt-in, e2e 단계서 1회 실증.
