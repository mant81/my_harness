# M14 작업결과서: F9 Docs(산출물) 소스 설정

> 상태: **완료·게이트 green·외부감사 R4·R5 2회 연속 HIGH 0.** 브랜치 `feat/harness-ui-v0.5`. 계획서 `docs/harness-ui/v0.6/todo/M14-F9-docs-sources.md`.

## 1. 작업 요약
- **목표:** F5 Docs 뷰어의 `docs/` 하드코딩을 제거하고 표시할 산출물 소스를 설정으로 지정(다중 등록+메뉴 on/off). 읽기전용 확장·config만 쓰기(I8 유지).
- **완료:** config additive(`docsSources`/`docsMenuEnabled`·per-leaf 복구)·소스 경로검증(DS1~DS8)·소스 인지 `/api/docs*`·`docsTree` base 파라미터화+TOCTOU 경화·Settings 소스편집기·Docs 다중소스 UI·nav 토글. 수용기준 **A113~A120** 전건.

## 2. 변경 파일
**서버:** `src/server/lib/config.ts`(docsSources/docsMenuEnabled additive·per-leaf·ConfigPatch·updateConfig RMW·`...obj` passthrough lock) · `src/server/lib/docssources.ts`(**신규**·DS1~DS6 검증·sourceId opaque) · `src/server/adapters/docs.ts`(`docsTree(projectRoot,base)` 파라미터화 + 진입부 anchor + walk **pre/post dev·ino 바인딩·null-sentinel 노드제외**) · `src/server/api/index.ts`(`GET /api/docs/sources`·`?source=`·`GET /api/docs/*?source=`·`POST /api/settings/docs-sources`·canonical 병합·토글 정합).
**웹:** `src/web/docs-sources.ts`(**신규**·에러매핑·행조작·dryRun) · `src/web/api.ts`(sources/source 헬퍼) · `src/web/docs-view.ts` · `src/web/screens.tsx`(DocsSourcesEditor·DocsBrowser·저장후 canonical 재동기화) · `src/web/App.tsx`(nav 토글) · `src/web/styles.css`.
**테스트(신규):** `test/{docssources.config,docssources.validate,docssourcesapi,docstree.toctou,webdocssources}.test.ts` + `test/config.test.ts`(passthrough lock)·`test/evals.test.ts`(H1 결정화).

## 3. 검증 결과
- 게이트: `tsc --noEmit` **PASS** · `npm run build` **PASS** · `vitest run` **792 passed / 1 skipped**(clean·exit 0).
- 신규 F9 스위트: config·validate·api·toctou·web 도합 **~110 테스트**. RED→GREEN: A113~A120 positive + 거부 스위트(DS1 루트/절대/`~`/`..`/UNC·DS2 제어문자·DS3 containment·DS4 심링크 base·DS5 denylist·DS6 개수/길이·**DS7 등록후 base 스왑·walk 검증↔readdir 레이스**·I8 회귀).
- 회귀 0(F3/F7/F8 공유 config 저장 경로·F5 뷰어 포함). 1 skip = 기존 projectroot 조건부.
- **flaky 주의:** WSL2 실서브프로세스 테스트(defedit/supervisor)가 직렬 run 최후미 CPU 기아로 간헐 30s 타임아웃 — 격리 실행 전건 PASS·F9 무관·비회귀.

## 4. 미해결 / 후속
- 설계 F9.2 라벨 기본값 `docs`(소문자) vs 계획/구현 `Docs` — 구현이 계획 채택(코스메틱·기능 무관).
- config I/O: `/api/docs*` 요청마다 `loadConfigFromDisk` — 로컬 허용 비용. mtime 캐시 최적화 = v0.7 노트.

## 5. 외부 리뷰 반영 (codex+agy·R1~R5)
- **R1**: 양 HIGH 0. codex LOW=중복 소스 canonical 병합 → 병합 키·저장 path를 `lexicalValidate(path).segs.join("/")`로 정규화(sourceId 안정·A115). **확인·수정.**
- **R2**: agy HIGH 0, codex HIGH=`docsTree` walk in-request TOCTOU(검증↔readdir 레이스) → walk에 pre/post dev·ino 바인딩+realpath 재검증(파일 열람 규율 이식). **확인·수정**(레이스 시뮬 4건).
- **R3**: agy 인프라 타임아웃, codex HIGH×2 — ①walk 레이스 노드 완전제외(빈 노드 잔존 → null-sentinel로 부모 push skip) **확인·수정**, ②config passthrough 미구현 주장 → **false-positive 기각**(`config.ts:77 ...obj` 이미 구현·lock 테스트 PASS·오케스트레이터 직접 검증).
- **R4·R5**: **codex+agy 양 엔진 HIGH 0**(DS1~DS8·TOCTOU·passthrough·경계 재검증). R5 codex LOW=저장후 폼 canonical 재동기화 → `setRows`/`setMenuOn`을 서버 응답으로 갱신. **확인·수정.**
- raw 판정·프롬프트: `_workspace/reviews/m14-code-r{1..5}_{codex,agy}.md`.
- <details><summary>핵심 교훈</summary>TOCTOU는 "요청 전 스왑"만이 아니라 "검증↔I/O 사이 레이스"까지 — 파일 열람의 pre/post dev·ino 바인딩 규율을 트리 walk에도 반드시 이식. 외부 리뷰어의 HIGH도 판정권자가 코드로 직접 검증(R3 HIGH-2 false-positive)해 무차별 수용 금지.</details>

## 6-2. 교훈 → 개선
- 계획서 A117이 "소스별 F5 DV 전건 재적용"을 명시했으나 초기 구현은 denylist/traversal만 재실증 → 외부감사가 XSS/바이너리 공백 지적. **경계 재사용도 "소스별 재실증 테스트 1건"을 계획 단계에서 못박을 것.**

## 7. 다음 단계 참조
- **미해결:** 없음(M14 DoD 충족). 라벨 대소문자·config I/O 캐시는 코스메틱/v0.7.
- **핵심 결정과 이유:** ① 소스 경로 = projectRoot 하위 상대·루트(`.`) 금지(임의 파일 브라우저 방지). ② `docsTree` walk TOCTOU는 파일 열람과 동일 pre/post dev·ino 바인딩으로 봉쇄(트리 walk는 openSafeFile이 못 덮음). ③ config passthrough(`...obj`)는 F3/F7/F8/F9 공유 불변식 — 이미 구현·lock 테스트로 가드.
- **다음 단계 = M15(F10 하네스 컨텍스트 관리+빌더·중대):** 착수 조건 = M14 커밋 완료. 주의 — F10 편집층은 F7 100% 재사용(신규 방어층 0)·읽기 멀티런타임(`.claude`·`.codex`·`.agents` 3 dot-dir·`node_modules` 대량 dir 차단 HR7)·빌드 초안 exec 메커니즘은 **P3 선검증 필수**(가정 위 구현 금지)·신규 정의 생성은 F7 재사용 아님(신규 구축). 중대 등급 = 단계마다 외부감사.
