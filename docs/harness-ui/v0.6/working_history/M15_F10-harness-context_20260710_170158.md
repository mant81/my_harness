# M15 작업결과서: F10 하네스 컨텍스트 관리 페이지 + 에이전트/스킬 빌더

> 상태: **완료·중대 등급·게이트 green·외부감사 R7·R8 2회 연속 HIGH 0.** 브랜치 `feat/harness-ui-v0.5`. 계획서 `docs/harness-ui/v0.6/todo/M15-F10-harness-context.md`. v0.6 마지막 마일스톤.

## 1. 작업 요약
- **목표:** 하네스 구성 컨텍스트(CLAUDE.md·AGENTS.md·`.claude`/`.codex`/`.agents` 에이전트·스킬)를 한 페이지에서 **멀티런타임 읽기 + Claude 편집 + 빌드**. 최대 공격면(신규 exec·읽기 화이트리스트 확장·신규 생성).
- **완료:** 멀티런타임 읽기(claude+codex+agy·HR1~HR7)·편집 F7 재사용(Claude만·HR6)·빌드 초안(HB1~HB8·no-auto-apply)·신규 생성(신규 구축)·Context 11번째 화면. 수용기준 **A121~A130** 전건.

## 2. 변경 파일
**서버 신규:** `src/server/lib/contextpaths.ts`(`deniedContextPath`·`classifyContextPath` HR1/HR2 2층 방어·MAX_CONTEXT_NODES·런타임 라벨) · `src/server/adapters/context.ts`(`contextTree` HR3/HR7 walk pre/post dev·ino·null-sentinel·node_modules 차단·`ensureCreatePath` HB5 신규 구축) · `src/server/lib/builddraft.ts`(`buildDraftArgv` HB2·주입 exec·`BuildGate` HB8·HOME/XDG/cwd 격리).
**서버 수정:** `src/server/api/index.ts`(`/api/context/*` 5라우트·게이트 순서) · `src/server/index.ts`(buildExec 주입) · `src/server/lib/exec.ts`(cwd/env 통과) · `src/server/lib/servefile.ts`(**additive** `renderableExt`·TOML 렌더·F5 기본값 보존) · `src/server/adapters/harness.ts`(agy 분기 additive).
**웹:** `src/web/context.ts`(신규·런타임 배지/필터·editDecision·미적용 초안 세션) · `src/web/api.ts`(context 헬퍼) · `src/web/screens.tsx`(Context 화면·F5 뷰어·F7 편집기 재사용) · `src/web/App.tsx`(11번째 nav) · `src/web/styles.css`.
**테스트(신규):** `test/{contextpaths,contexttree,contextapi,builddraft,webcontext,webcontextapi}.test.ts`.

## 3. 검증 결과
- 게이트: `tsc --noEmit` **PASS** · `npm run build` **PASS** · `vitest run` **874 passed / 1 skipped**(77 files·exit 0·flaky 0).
- 신규 F10 스위트 ~90 테스트. RED→GREEN: A121~A130 + 거부 스위트(멀티런타임 읽기 탈출·`.claude`/`.codex`/`.agents` 외 dot·홈전역·심링크·node_modules·트리 walk 레이스·Codex/agy/GEMINI.md 편집 409·빌드 주입/동시 429·신규생성 경로탈출/이름충돌·쓰기 경계 I8).
- 회귀 0(F5 뷰어·F7 편집기·harness 인벤토리·M14 F9 포함).

## 4. 미해결 / 후속
- **빌드 인증 한계(v0.6):** 초안 생성이 HOME/XDG 격리로 OAuth/keychain 미접근 → **`ANTHROPIC_API_KEY` 인증 필요**. 보안(exfil 원천차단) > 편의로 판단. v0.7에서 인증 격리 완화 검토.
- **v0.7:** 서버 큐레이트 읽기전용 컨텍스트를 빌드 초안 prompt에 데이터 주입(현재는 domain/role만)·풀 팩토리 오케스트레이션·Codex/agy 편집.
- **잔여 LOW(보안-auditor·비경계):** create leaf 비-O_EXCL(cross-process TOCTOU·out-of-scope)·draft read tools는 이제 `--tools ""`로 무의미화.

## 5. 외부 리뷰 반영 (codex+agy·R1~R8)
- **R1**: agy HIGH 0, codex HIGH=빌드 초안 exec에 Read/Grep/Glob 허용→주입 exfil(HB3 위반) → **도구 제거**. **확인·수정.**
- **R2**: codex HIGH=`--allowedTools` 부재≠도구 0 → **HOME/XDG/cwd 빈 temp 격리** + 명시 차단 시도. **확인·수정.**
- **R3**: 양 엔진 HIGH 0(1차 clean).
- **R4**: 양 엔진 HIGH 0, codex MED=실패 요청(403/400) 쿨다운 소비 → **게이트 acquire를 검증·edit-gate 뒤로**. **확인·수정.**
- **R5**: codex HIGH=denylist 불완전(LS/TodoRead/ExitPlanMode 누락)·safe-mode 미보완 → allowlist-empty 시도. **확인·수정.**
- **R6**: codex HIGH=**`--allowedTools ""`≠deny-all(의미론 반대)** — 공식 CLI reference 근거. → **설치 `claude --help` 실측**으로 정정: **`--tools ""`=built-in 도구 전체 비활성**이 정답(부수: `--disallowedTools "LS"`는 "no known tool" 경고=R5 열거 무효). **확인·실측 정정.**
- **R7·R8**: **codex+agy 양 엔진 HIGH 0**(exec 샌드박스·멀티런타임·트리·신규생성·쓰기경계 재검증). 2회 연속.
- 최종 exec argv: `claude -p --output-format text --permission-mode plan --safe-mode --tools "" --disallowedTools "*" -- <prompt>` + HOME/XDG/cwd 빈 temp 격리 + 최소 env.
- raw: `_workspace/reviews/m15-code-r{1..8}_{codex,agy}.md`.
- <details><summary>핵심 교훈</summary>agentic CLI를 exec surface로 쓸 때 "도구 비활성"은 **플래그 의미론을 설치 버전 `--help`로 실측**해야 한다(docs·가정 금지·`--allowedTools`는 사전승인이지 제한 아님). deny-list 열거는 본질적으로 불완전 → **allowlist/`--tools ""` default-deny + 환경 격리 심층방어**. 외부 리뷰어 HIGH도 판정권자가 코드·CLI로 직접 검증(R3 config-passthrough false-positive vs R6 실측 확증).</details>

## 6-2. 교훈 → 개선
- 빌드 exec 샌드박스가 R1→R6에 걸쳐 6번 심화됐다. **exec surface 설계는 착수 전 P3에서 "도구 비활성 플래그 실측"을 필수 게이트로** 명시했어야(계획서 P3는 "메커니즘 선검증"만·플래그 의미론 실측은 누락). v0.7 exec 기능은 이 교훈 반영.

## 7. 다음 단계 참조
- **미해결:** 없음(M15 DoD 충족·A121~A130 전건·R7·R8 2회 연속 HIGH 0). 빌드 ANTHROPIC_API_KEY 한계·v0.7 후속은 위 §4.
- **핵심 결정과 이유:** ① 멀티런타임 읽기는 **3 dot-dir 정밀 화이트리스트 + 독립 `deniedContextPath`**(전역 DENY 미수정·F5 방어 보존). ② 편집은 **Claude만**(F7 재사용)·Codex/agy 409(duo drift·정규화 미비). ③ 빌드 초안 exec = **`--tools ""` deny-all(실측 확정) + 환경/cwd 격리** 4층 심층방어·no-auto-apply. ④ 신규 생성 = 신규 구축(F7은 수정 전용).
- **다음 단계 = v0.6 M7~M15 전 마일스톤 완료.** 후속: (a) 전체 e2e 통합(F2~F10 화면·경계면)·(b) push(현재 `_workspace/.autonomous-push` 미설정으로 대기)·(c) v0.7 백로그(빌드 컨텍스트 주입·Codex/agy 편집·풀 팩토리·crypto ledger). M14(F9)·M15(F10) 커밋 완료 상태에서 사용자 승인 후 push.
